import { ActivarMenuDirective } from './activar-menu.directive';

describe('ActivarMenuDirective', () => {
  it('should create an instance', () => {
    const directive = new ActivarMenuDirective();
    expect(directive).toBeTruthy();
  });
});
